import 'package:flutter/material.dart';

class RiwayahPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Riwayat Hafalan'),
      ),
      body: Center(
        child: Text('Memuat Halaman Riwayat Hafalan!'),
      ),
    );
  }
}
