import 'package:flutter/material.dart';

class HafalanPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Hafalan'),
      ),
      body: Center(
        child: Text('Memuat Halaman Setor Hafalan!'),
      ),
    );
  }
}
